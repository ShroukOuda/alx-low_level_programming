#include "lists.h"
/**
 * get_nodeint_at_index - check the code.
 * @head: it's a head
 * @index: it's an index
 * Return: Always 0.
 */
listint_t *get_nodeint_at_index(listint_t *head, unsigned int index)
{
}
